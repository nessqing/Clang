#include <stdio.h>

int main() {
    int num = 5;
    int *pNum = &num;

    printf("num value is %d\n", a);
    printf("&num value is %p\n", &a);
    printf("pNum is %p\n", p);
    printf("&pNum is %p\n", &p);
    printf("*pNum is %d\n", *p);

    return 0;
}
